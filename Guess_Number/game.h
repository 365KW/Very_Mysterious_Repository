#ifndef CUSTOM_LIB_GAME_H
#define CUSTOM_LIB_GAME_H
#define ll long long
#include <string>
#include <random>
#include <cstdio>
#include <fstream>
#include <iostream>

class custom_random {
protected:
private:
public:
    ll static custom_random_spawner(ll a, ll b);
    ll static stoi(const std::string& s);
};

#endif //CUSTOM_LIB_GAME_H
